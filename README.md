
Database Setup:

Create MySQL database:
CREATE DATABASE gameapp;

MySQL credentials used:
Username: root
Password: admin

If your credentials are different, update DatabaseConnection.java in util package.